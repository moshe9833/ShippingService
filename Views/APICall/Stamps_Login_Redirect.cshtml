@using static CourierRates.Controllers.APICallController;
@model List<CourierRates.Controllers.APICallController.Root>
@{
    ViewData["Title"] = "Stamps Rating";
    Layout = null;
    var model_Rate = Model.FirstOrDefault();
    //Rateresponse Rate_Model = Model.RateResponse;
}

<link href="~/lib/bootstrap/dist/css/bootstrap.min.css" rel="stylesheet" />
<link href="~/lib/bootstrap/dist/css/bootstrap.rtl.min.css" rel="stylesheet" />
<link href="~/lib/bootstrap/dist/css/bootstrap.rtl.css" rel="stylesheet" />
<link href="~/lib/bootstrap/dist/css/bootstrap.css" rel="stylesheet" />

<script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
<script src="~/lib/bootstrap/dist/js/bootstrap.bundle.js"></script>
<script src="~/lib/bootstrap/dist/js/bootstrap.esm.min.js"></script>
<script src="~/lib/bootstrap/dist/js/bootstrap.esm.js"></script>
<script src="~/lib/bootstrap/dist/js/bootstrap.min.js"></script>
<script src="~/lib/bootstrap/dist/js/bootstrap.js"></script>
<h2>Service and Rate List</h2>
<h4><span style="color:red">Below is rate list </span></h4>
<a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Index">Click here to get back on master page</a>
<head>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.1.3/css/bootstrap.min.css"
          rel="stylesheet">
</head>
<body>
    <div>
        <input type="button"
               value="Go Somewhere Else"
               onclick="location.href='<%: Url.Action("Action", "Controller" ) %>'" />
    </div>
    <div id="maindiv" >
        <table class="table table-striped sampleTable" id="table_id">
            <thead>
                <tr>
                    <th scope="col">Carrier</th>
                    <th scope="col">Service Type</th>
                    <th scope="col">Packaging Type</th>
                    <th scope="col">Shipment Cost</th>
                    <th scope="col">Cost Details</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td scope="row">@item.carrier</td>
                        <td>@item.service_type</td>
                        <td>@item.packaging_type</td>
                        <td>@(@item.shipment_cost.currency + " " + @item.shipment_cost.total_amount)</td>
                        <td>
                            @*<table class="table table-striped table-border">
                                <thead>
                                    <tr>
                                        <th>Fee_code</th>
                                        <th>Fee Type</th>
                                        <th>Amount</th>
                                    </tr>
                                </thead>
                                @foreach (var cost in model_Rate.shipment_cost.cost_details)
                                {
                                    <tr>
                                        <td>@cost.fee_code</td>
                                        <td>@cost.fee_type</td>
                                        <td>@cost.amount</td>
                                    </tr>

                                }
                            </table>*@

                        </td>

                    </tr>
                }
               
            </tbody>
        </table>
    </div>
</body>
